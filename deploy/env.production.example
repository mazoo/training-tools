STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_REDIRECT_URI=https://training.example.com/auth/strava/callback
FRONTEND_URL=https://training.example.com
DATABASE_URL=sqlite+aiosqlite:////var/lib/training-tools/training_tools.db
RATE_LIMIT_STATE_PATH=/var/lib/training-tools/rate_limit_state.json
# Generate both with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=
BACKFILL_SECRET=
# Comma-separated Strava athlete IDs allowed to complete OAuth in the first production phase.
ALLOWED_ATHLETE_IDS=
HOME_LAT=
HOME_LNG=
